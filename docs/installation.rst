============
Installation
============

At the command line::

    $ easy_install ecommerce_processors

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ecommerce_processors
    $ pip install ecommerce_processors
